/vendor/
/.phpunit.cache/
.phpunit.result.cache

# Deployment SmartUCF passphrase (ZIP packaging only — keep secrets/ structure tracked)
/secrets/smartucf-key.php

# SmartUCF certificate / private-key material (keep keys/ protection files tracked)
/keys/*.pem
/keys/.incoming/
/keys/.ssl_state.json
/keys/.sync.lock
